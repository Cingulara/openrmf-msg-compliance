{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/openrmf-msg-compliance.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}